def generate_explanation(image_path: str, method: str, model=None) -> str:
    """
    STUB — Replace with Grad-CAM / LIME / SHAP logic.
    Returns path to placeholder heatmap image.
    """
    # For now, we simply return a default static path.
    # In a real app, this would generate an image and save it to storage.
    return "static/placeholder_heatmap.png"
